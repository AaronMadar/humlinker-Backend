import { Global, Module } from '@nestjs/common';

/**
 * Database connectivity and ORM providers will be registered here.
 */
@Global()
@Module({
  providers: [],
  exports: [],
})
export class DatabaseModule {}
